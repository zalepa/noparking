<% content_for(:title, "All reports") %>

<div data-controller="officer-location"
     data-officer-location-lat-value="<%= @coordinates&.first %>"
     data-officer-location-lng-value="<%= @coordinates&.last %>">

  <header class="mb-6">
    <h1 class="text-3xl font-bold tracking-tight text-gray-900">All reports</h1>
    <p class="mt-2 text-sm text-gray-600">
      <% if @coordinates %>
        Sorted by distance from your current location.
      <% else %>
        Sorted by most recent.
      <% end %>
    </p>
  </header>

  <% unless @coordinates %>
    <div class="mb-6 rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm flex items-center justify-between gap-3">
      <span class="text-amber-800">Share your location to sort by distance.</span>
      <button type="button" data-action="officer-location#request"
              class="rounded-lg bg-amber-600 px-3 py-1.5 text-xs font-semibold text-white hover:bg-amber-700">
        Share location
      </button>
    </div>
  <% end %>

  <section class="bg-white rounded-2xl border border-gray-200 overflow-hidden">
    <% if @issues.any? %>
      <ul class="divide-y divide-gray-100" data-officer-location-target="list">
        <% @issues.each do |issue| %>
          <%= render "officer/issues/issue_row", issue: issue, coordinates: @coordinates %>
        <% end %>
      </ul>
    <% else %>
      <p class="px-5 py-8 text-center text-sm text-gray-500">No reports yet.</p>
    <% end %>
  </section>
</div>
