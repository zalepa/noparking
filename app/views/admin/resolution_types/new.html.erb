<% content_for(:title, "New resolution type") %>

<header class="mb-6">
  <%= link_to "← Resolution types", admin_resolution_types_path, class: "text-sm font-medium text-gray-600 hover:text-gray-900" %>
  <h1 class="mt-2 text-2xl font-bold tracking-tight text-gray-900">New resolution type</h1>
</header>

<div class="bg-white rounded-2xl border border-gray-200 p-6 max-w-xl">
  <%= render "form", resolution_type: @resolution_type %>
</div>
