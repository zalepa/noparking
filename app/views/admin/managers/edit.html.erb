<% content_for(:title, "Edit manager") %>

<div class="max-w-xl">
  <header class="mb-6">
    <h1 class="text-3xl font-bold tracking-tight text-gray-900">Edit manager</h1>
    <p class="mt-2 text-sm text-gray-600"><%= @manager.display_name %></p>
  </header>
  <div class="bg-white rounded-2xl border border-gray-200 p-6 sm:p-8">
    <%= render "form", manager: @manager %>
  </div>
</div>
